"use client";
import { useState } from "react";
import { signIn } from "next-auth/react";
import { useRouter } from "next/navigation";
import { Card, Input, Label, Button } from "@/components/ui";
export default function Login(){const [email,setEmail]=useState("admin@mohonto.com"),[password,setPassword]=useState("Admin@123"),[error,setError]=useState("");const router=useRouter();return <div className="flex min-h-screen items-center justify-center bg-gradient-to-br from-teal-50 to-slate-100 p-4"><Card className="w-full max-w-md p-8"><div className="mb-8 text-center"><div className="text-2xl font-bold text-teal-700">Mohonto Dental Care</div><p className="mt-1 text-sm text-slate-500">Sign in to clinic management</p></div><form className="space-y-4" onSubmit={async e=>{e.preventDefault();const r=await signIn("credentials",{email,password,redirect:false});if(r?.ok)router.push("/dashboard");else setError("Invalid email or password.");}}><div><Label>Email</Label><Input value={email} onChange={e=>setEmail(e.target.value)} type="email"/></div><div><Label>Password</Label><Input value={password} onChange={e=>setPassword(e.target.value)} type="password"/></div>{error&&<p className="text-sm text-red-600">{error}</p>}<Button className="w-full">Sign in</Button></form><p className="mt-5 text-center text-xs text-slate-400">Demo: admin@mohonto.com / Admin@123</p></Card></div>}
