"use client";
import { useEffect,useState } from "react"; import {Card,Badge} from "@/components/ui";
export default function Dashboard(){const [d,setD]=useState<any>();useEffect(()=>{fetch("/api/dashboard").then(r=>r.json()).then(setD)},[]);if(!d)return <div>Loading...</div>;const stats=[["Active Patients",d.patients],["Case Histories",d.cases],["Prescriptions",d.prescriptions],["Today's Appointments",d.todayAppointments],["Today's Collection",`৳ ${d.todayCollection.toFixed(2)}`]];return <div><div className="mb-6"><h1 className="text-2xl font-bold">Dashboard</h1><p className="text-sm text-slate-500">Overview of Mohonto Dental Care</p></div><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-5">{stats.map(([a,b])=><Card key={a as string} className="p-5"><div className="text-sm text-slate-500">{a}</div><div className="mt-2 text-2xl font-bold">{b}</div></Card>)}</div><Card className="mt-6 p-5"><div className="mb-4 flex justify-between"><h2 className="font-semibold">Recent Patients</h2><Badge>Live</Badge></div><div className="divide-y">{d.recentPatients.map((p:any)=><div key={p.id} className="flex justify-between py-3 text-sm"><span>{p.name}</span><span className="text-slate-500">{p.phone}</span></div>)}</div></Card></div>}
