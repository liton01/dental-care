"use client";
import {useEffect,useState} from "react";import {Card,Input,Label,Button} from "@/components/ui";
export default function Greetings(){const [templates,setTemplates]=useState<any[]>([]),[patients,setPatients]=useState<any[]>([]),[f,setF]=useState<any>({name:"",subject:"",body:"",channel:"EMAIL"}),[send,setSend]=useState<any>({patientId:"",templateId:""});const load=()=>fetch("/api/greetings/templates").then(r=>r.json()).then(setTemplates);useEffect(()=>{load();fetch("/api/patients").then(r=>r.json()).then(setPatients)},[]);const save=async(e:any)=>{e.preventDefault();await fetch("/api/greetings/templates",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(f)});setF({name:"",subject:"",body:"",channel:"EMAIL"});load()};const doSend=async()=>{const r=await fetch("/api/greetings/send",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(send)});const d=await r.json();alert(d.status==="SENT"?"Message sent":"Message processed: "+(d.error||d.status))};return <div><h1 className="mb-6 text-2xl font-bold">Greetings</h1><div className="grid gap-6 xl:grid-cols-2"><Card className="p-5"><h2 className="mb-4 font-semibold">Template Management</h2><form onSubmit={save} className="space-y-3">{["name","subject"].map(k=><div key={k}><Label>{k}</Label><Input value={f[k]} onChange={e=>setF({...f,[k]:e.target.value})}/></div>)}<div><Label>Channel</Label><select className="input" value={f.channel} onChange={e=>setF({...f,channel:e.target.value})}><option>EMAIL</option><option>SMS</option><option>WHATSAPP</option></select></div><div><Label>Body</Label><textarea className="input min-h-32" value={f.body} onChange={e=>setF({...f,body:e.target.value})} placeholder="Use {{patientName}}, {{phone}}, {{patientNo}}"/></div><Button>Save Template</Button></form></Card><Card className="p-5"><h2 className="mb-4 font-semibold">Send Greeting</h2><div className="space-y-3"><div><Label>Patient</Label><select className="input" value={send.patientId} onChange={e=>setSend({...send,patientId:e.target.value})}><option value="">Select</option>{patients.map(p=><option key={p.id} value={p.id}>{p.name}</option>)}</select></div><div><Label>Template</Label><select className="input" value={send.templateId} onChange={e=>setSend({...send,templateId:e.target.value})}><option value="">Select</option>{templates.map(t=><option key={t.id} value={t.id}>{t.name} ({t.channel})</option>)}</select></div><Button className="w-full" onClick={doSend}>Send Message</Button></div><div className="mt-8 space-y-2">{templates.map(t=><div key={t.id} className="rounded-xl border p-3"><div className="font-medium">{t.name}</div><div className="text-xs text-slate-500">{t.channel}</div></div>)}</div></Card></div></div>}
