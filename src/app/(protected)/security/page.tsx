"use client";
import {useEffect,useState} from "react";import {Card,Input,Label,Button} from "@/components/ui";
export default function Security(){const [users,setUsers]=useState<any[]>([]),[roles,setRoles]=useState<any[]>([]),[permissions,setPermissions]=useState<any[]>([]),[menus,setMenus]=useState<any[]>([]),[f,setF]=useState<any>({name:"",email:"",password:"Admin@123",roleId:""});const load=()=>{fetch("/api/security/users").then(r=>r.json()).then(setUsers);fetch("/api/security/roles").then(r=>r.json()).then(setRoles);fetch("/api/security/permissions").then(r=>r.json()).then(setPermissions);fetch("/api/security/menus").then(r=>r.json()).then(setMenus)};useEffect(load,[]);const save=async(e:any)=>{e.preventDefault();await fetch("/api/security/users",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(f)});setF({...f,name:"",email:""});load()};return <div><h1 className="mb-6 text-2xl font-bold">Security</h1><div className="grid gap-6 xl:grid-cols-2"><Card className="p-5"><h2 className="mb-4 font-semibold">Users</h2><form onSubmit={save} className="grid gap-3 md:grid-cols-2">{["name","email","password"].map(k=><div key={k}><Label>{k}</Label><Input type={k==="password"?"password":"text"} value={f[k]} onChange={e=>setF({...f,[k]:e.target.value})} required/></div>)}<div><Label>Role</Label><select className="input" value={f.roleId} onChange={e=>setF({...f,roleId:e.target.value})}>{roles.map(r=><option key={r.id} value={r.id}>{r.name}</option>)}</select></div><div className="md:col-span-2"><Button>Create User</Button></div></form><div className="mt-6 divide-y">{users.map(u=><div className="flex justify-between py-3 text-sm" key={u.id}><span>{u.name}<small className="ml-2 text-slate-400">{u.email}</small></span><span>{u.roles.map((x:any)=>x.role.name).join(", ")}</span></div>)}</div></Card><Card className="p-5"><h2 className="font-semibold">Roles & Permissions</h2><div className="mt-4 grid gap-2">{roles.map(r=><div key={r.id} className="rounded-xl border p-3"><b>{r.name}</b><div className="mt-1 text-xs text-slate-500">{r.permissions.length} role permissions</div></div>)}</div><h2 className="mt-6 font-semibold">Menus</h2><div className="mt-3 space-y-2">{menus.map(m=><div key={m.id} className="rounded-lg bg-slate-50 p-2 text-sm">{m.title} <span className="text-slate-400">{m.path}</span></div>)}</div><h2 className="mt-6 font-semibold">Permission Catalog</h2><div className="mt-3 grid grid-cols-2 gap-2 text-xs">{permissions.map(p=><div key={p.id} className="rounded-lg border p-2">{p.code}</div>)}</div></Card></div></div>}
